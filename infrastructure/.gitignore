# Terraform
# terraform.tfvars
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Variables sensibles
dev.tfvars
prod.tfvars
# *.tfvars # j'ai enlevé le secret mdp et ajouté dans github secret donc je peux le commit


# Clés SSH
*.pem
key/



# Dans bootstrap/ — ne pas commit le state local du bootstrap
bootstrap/.terraform/
bootstrap/terraform.tfstate
bootstrap/terraform.tfstate.backup
bootstrap/.terraform.lock.hcl

script.S3.dynamoDB.txt

